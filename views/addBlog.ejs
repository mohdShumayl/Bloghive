<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('./partials/head') %>
    <title>Add Blog</title>
    <style>
        body {
            background: linear-gradient(135deg, #ffffff, #000000);
            color: #fff;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            min-height: 100vh;
        }

        .form-container {
            max-width: 700px;
            margin: 3rem auto;
            padding: 2rem;
            background: rgba(0, 0, 0, 0.5);
            border-radius: 15px;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.3);
        }

        .form-container h1 {
            text-align: center;
            margin-bottom: 2rem;
            font-size: 2.5rem;
            color: #fff;
        }

        .form-group {
            margin-bottom: 1.5rem;
        }

        label {
            font-weight: 600;
            color: #ededed;
        }

        input[type="text"],
        textarea,
        input[type="file"] {
            width: 100%;
            padding: 0.8rem 1rem;
            border-radius: 10px;
            border: 1px solid #ccc;
            outline: none;
            font-size: 1rem;
        }

        input[type="text"]:focus,
        textarea:focus,
        input[type="file"]:focus {
            border-color: orange;
            box-shadow: 0 0 5px rgba(255, 165, 0, 0.5);
        }

        textarea {
            resize: vertical;
        }

        .btn-submit {
            display: block;
            width: 100%;
            padding: 0.8rem;
            border: none;
            border-radius: 25px;
            background-color: orange;
            color: #fff;
            font-size: 1.1rem;
            font-weight: 500;
            cursor: pointer;
            transition: 0.2s;
        }

        .btn-submit:hover {
            background-color: darkorange;
        }
    </style>
</head>

<body>
    <%- include('./partials/nav') %>

    <div class="form-container">
        <h1>Add New Blog</h1>
        <form action="/blog/" method="post" enctype="multipart/form-data">
            <div class="form-group">
                <label for="coverImage">Cover Image</label>
                <input type="file" id="coverImage" name="coverImage">
            </div>

            <div class="form-group">
                <label for="blogTitle">Title</label>
                <input type="text" id="blogTitle" name="title" placeholder="Enter blog title" required>
            </div>

            <div class="form-group">
                <label for="blogBody">Body</label>
                <textarea id="blogBody" name="body" rows="6" placeholder="Write your blog content..." required></textarea>
            </div>

            <button type="submit" class="btn-submit">Add Blog</button>
        </form>
    </div>

    <%- include('./partials/scripts.js') %>
</body>

</html>
